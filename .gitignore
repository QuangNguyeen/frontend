# Environment variables
.env
.env.*
!.env.example
# Production build vars (VITE_* are public — baked into the client bundle —
# so they are safe to commit; Cloudflare's build reads this file).
!.env.production

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Review snapshot for thesis defense (chia sẻ riêng, không commit)
phan-bien-review/

# Internal project docs (SRS, use-cases, infographics) — không commit
docs/

# Local tooling / AI assistant & generated state (không commit)
.agent/
.claude/
.codegraph/
.cursor/
.gemini/
.wrangler/
